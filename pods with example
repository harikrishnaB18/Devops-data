apiVersion: v1
kind: Pod
metadata:
  name: mytestapp-pod
  labels:
      app: mytestapp
      type: front-end
spec:
  containers:
    - name: nginx-container
      image: nginx
      
      
      
 Now run the below command.
 #kubectl create -f Pod-definition.yml
 If you want to modify in your definition file and then you want to run again. Use below command.
 #kubectl apply -f Pod-definition.yml
 You can use kubectl edit command also.
 #kubectl edit pod <pod name>
 you can delete the pod
 #kubectl delete pods <pod name>
 you can delete the pods at a time 
 #kubectl delete pods <pod names eg:1 2 3>
